# Web
flask==3.1.*
jinja2>=3.1

# Serveur WSGI de production (lot 4.2 d'AUDIT.md §9 / volet WSGI du test T15).
# L'application tournait jusqu'ici sur le serveur de developpement Werkzeug.
# Version EPINGLEE sur la serie reellement testee en local : la configuration
# de demarrage du Dockerfile (--workers 1 --threads N, worker gthread) est
# validee contre celle-ci et contre aucune autre.
# CONTRAINTE : toujours --workers 1 (cf. l'en-tete de run.py).
gunicorn==26.0.*

# Database
sqlalchemy==2.0.*

# Scraping (scrapling[all] pulls patchright, browserforge, curl_cffi, etc.)
scrapling[all]>=0.2

# Downloads
httpx>=0.27
ffmpeg-python>=0.2

# Google Drive
google-api-python-client>=2.100
google-auth-oauthlib>=1.2
google-auth-httplib2>=0.2

# Scheduler
apscheduler==3.10.*

# Utils
python-dotenv>=1.0
loguru>=0.7
nanoid>=2.0
